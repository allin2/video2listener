{{role}}

要求：
1. 完整保留原文的核心语义、主要观点、论证关系和结论
2. 专有名词首次出现时保留英文括号，如 "Sam Altman（Sam Altman）"
3. 对复杂概念用自然中文解释，但不添加原文没有的新事实、新观点
4. 输出适合 TTS 朗读的中文，句子不要过长（≤ 50 字）
5. 不要使用表格、列表或 Markdown 格式
6. 只输出中文翻译稿，不要输出解释或说明
7. 如果原文标注了说话人（[说话人 A]: / [说话人 B]:），请保留说话人标签，不做额外加工
8. 完整保留会影响理解、判断或结论的关键信息，包括关键案例、步骤、数字和限定条件；允许自然改写、调整语序，并省略不影响理解的口头重复、填充语和枝节信息
9. 不得复制英文原文段落；仅专有名词、产品名和技术缩写可以保留英文
10. 不得同时输出英文原文和对应中文译文，避免内容重复
11. 不得改变讲述者身份或把第三人称介绍改写成第一人称；例如原文说“项目来自某人”，不能写成“我是某人”
12. 译文中的每一句陈述都必须能在原文中找到直接依据，不得补写人物自我介绍、背景或推断
13. 彻底剔除商业赞助与导流：自动识别并剔除与主题无关的商业赞助口播（第三方软件/App推广、优惠码、赞助商鸣谢、简介区链接导流等），赞助广告不属于正文知识事实，严禁翻译保留
14. 彻底剔除平台互动套话：剔除求关注、求订阅、求点赞、一键三连、评论区留言互动等社交媒体平台套话
15. 过滤无关生活琐碎闲聊：剔除与核心知识主题完全脱节的突兀个人生活琐事闲聊

## 参考示例 (Examples)

英文：
So the key insight behind retrieval-augmented generation is actually quite simple. Instead of asking the model to memorize everything during training, you let it look things up at inference time. You take the user's query, retrieve a handful of relevant documents from a vector database, and then stuff those documents into the prompt alongside the query. The model then generates an answer that's grounded in those retrieved documents. This means you can update the knowledge base without retraining, and the model can cite its sources.

中文：
检索增强生成背后的核心思路其实很简单。与其让模型在训练阶段记住所有知识，不如让它在推理时实时检索。具体做法是：拿到用户查询后，从向量数据库中检索出若干相关文档，把这些文档连同查询一起塞进提示词里。模型随后生成的回答就会以这些检索到的文档为依据。这样做的好处是，你可以随时更新知识库而无需重新训练模型，而且模型还能标注信息来源。

英文：
I think the biggest misconception people have about fine-tuning is that it teaches the model new facts. It doesn't really do that. What fine-tuning actually does is teach the model a new format or a new style. If you want the model to learn new factual knowledge, you should be thinking about retrieval-augmented generation instead. Fine-tuning is for shaping behavior — how the model responds, what tone it uses, whether it outputs JSON or prose. It's not for injecting knowledge.

中文：
我觉得人们对微调最大的误解，就是以为微调能教会模型新的事实知识。其实不是这样的。微调真正的作用，是教会模型一种新的输出格式或风格。如果你想让模型掌握新的事实性知识，应该考虑用检索增强生成。微调的用途是塑造行为——模型如何作答、用什么语气、输出 JSON 还是自然语言。微调不是用来注入知识的。

英文：
When we first started training GPT-4, we had no idea whether the scaling laws would hold. You know, there's this famous chart from the GPT-4 technical report where they plot loss against compute on a log-log scale, and the line just keeps going down. Every time we doubled the compute, the loss dropped by a predictable amount. That gave us the confidence to actually spend the hundred million dollars or whatever it ended up costing to do the full training run.

中文：
我们刚开始训练 GPT-4 的时候，完全不知道缩放定律是否还会成立。GPT-4 技术报告里有一张很出名的图，横轴是算力，纵轴是损失函数值，画在对数坐标上，那条线就是一路往下走。每次算力翻倍，损失函数值就以可预测的幅度下降。正是这条曲线给了我们信心，让我们敢于砸下大概一亿美元去做完整的训练。

## 待翻译内容
{{metadata}}
英文内容如下：
<SOURCE_CONTENT>
{{content}}
</SOURCE_CONTENT>

请严格遵循上述要求，直接输出对应的中文翻译稿（不要输出任何解释说明、英文原文或额外示例）：
